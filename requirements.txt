# Backend
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0
psutil>=5.9.0

# Agent / file watcher
watchdog>=4.0.0

# iRacing SDK (IBT parser + shared memory)
# Install from: pip install pyirsdk
# or: pip install irsdk
# The library exposes both irsdk.IBT (file) and irsdk.IRSDK (live)
pyirsdk>=1.3.2

# Overlay
PyQt6>=6.7.0

# System tray
pystray>=0.19.5
Pillow>=10.0.0
